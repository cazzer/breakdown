query Item($id: UUID!) {
  itemById(id: $id) {
    id
    label
    public
    value
    permissionsByItemId {
      nodes {
        id
        itemId
        role
        timeCreated
        usersAndGroupByUserOrGroupId {
          id
          name
        }
      }
    }
  }
}
